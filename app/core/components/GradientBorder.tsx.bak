import { Flex } from "@chakra-ui/react"

const GradientBorder = (props) => {
  const { variant, children, ...rest } = props
  return (
    <Flex
      p="2px"
      justify="center"
      align="center"
      bg="radial-gradient(69.43% 69.43% at 50% 25%, #FFFFFF 25%, rgba(255, 255, 255, 0) 100%),
          radial-gradient(60% 51.57% at 0% 0%, #009A4C 0%, rgba(88, 44, 255, 0) 100%),
          radial-gradient(54.8% 53% at 25% 25%, #151515 0%, rgba(21, 21, 21, 0) 100%)"
      {...rest}
    >
      {children}
    </Flex>
  )
}

export default GradientBorder
